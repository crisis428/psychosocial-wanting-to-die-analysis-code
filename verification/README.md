# Aggregate verification outputs

This folder contains **non-participant-level aggregate summaries only**. It is included so readers can cross-check the principal machine-learning performance results without the restricted checkpoint archive.

Excluded from the public repository are participant-level predictions, observed outcomes by participant, split indices, SHAP arrays tied to individual rows, and bulky fold-level checkpoint artifacts.
